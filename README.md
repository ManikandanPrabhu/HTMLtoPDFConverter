<!-- Dependencies:

1)	npm install fs --save
2)	npm install express --save
3)	npm install html-to-pdf --save
4)	npm install body-parser --save
5)	npm install path --save



To Build and Test the APP:

	1) Run "npm install"
	2) To make a Get Call Hardcoding the HTML in app.js file use lines 23 to 33 in sample.js, commenting the lines 10 to 20 
						(or)
 		To make a post Call on submit, comment lines 23 to 33 in sample.js, uncomment the lines 10 to 20
 -->